삭제된 `benelog/h2demo` 저장소의 `h2-boot-1.5.7` branch (Spring Boot 1.5.7 + Tomcat 8.5 데모) 에서
master branch 와 내용이 달랐던 파일만 원래 경로 그대로 보관한 것이다.

나머지 파일은 [`../h2-demo/`](../h2-demo/) 의 master 스냅샷과 같다. 이 버전을 실행하려면
`../h2-demo/` 를 복사한 뒤 이 디렉터리의 파일로 같은 경로를 덮어쓴다.

`docs/` (발표 자료) 는 이 branch 에도 있었지만, 그 최신본이 이미
[`20171126-http2-java/`](../../../20171126-http2-java/) 로 옮겨져 있으므로 이미지는 여기에 다시 두지 않았다.
초안 문서 `docs/http2-java.md` 만 [`../http2-java-draft.md`](../http2-java-draft.md) 로 옮겨 보관했다.
